# 管理治理数据与文档资产分离

成员、项目、直接授权、会话状态和管理审计需要可查询、可约束的运行数据，而 Git 中的 Markdown 必须保持干净、可审计且可由发布流程独立版本化。我们决定将全部管理治理资料以 SQLite 为唯一事实来源，项目的 Git 侧 `meta.json` 仅保存 `dependencies`；不在 Markdown 或 `meta.json` 中重复保存成员、负责人、状态、角色或审计记录。这个选择牺牲了“所有资料都在 Git 中”的表面一致性，换取了 ACL、即时会话失效、不可变审计和项目生命周期的可靠约束。